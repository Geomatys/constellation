#!/bin/bash

# ---------------------------------------------------------------------------
# Prints Coordinate Reference System (CRS) objects in "Well Known Text" (WKT)
# format. The CRS objects are designed by a "authority:code" tuple.
# ---------------------------------------------------------------------------


# Initialization common to all scripts
export BASE_DIR=`dirname $0`/..
source $BASE_DIR/etc/setenv.sh

# Runs Java commands
export CLASSPATH=.
export CLASSPATH=$CLASSPATH:$JARS/gt2-epsg-hsql-$GEOTOOLS_VERSION.jar
export CLASSPATH=$CLASSPATH:$JARS/gt2-epsg-extension-$GEOTOOLS_VERSION.jar

java $OPTS org.geotools.referencing.CRS $*
