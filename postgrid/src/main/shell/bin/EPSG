#!/bin/sh

# -----------------------------------------------------------
# Pour un nombre arbitraire de codes EPSG, affiche les
# objets correspondants sous forme de "Well Known Text" (WKT)
# -----------------------------------------------------------

`dirname $0`/CRS -colors -authority=EPSG $*
